services:
  berlin-insider:
    build: .
    image: berlin-insider:latest
    container_name: berlin-insider
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./.data:/app/.data
    env_file:
      - path: .env
        required: false
